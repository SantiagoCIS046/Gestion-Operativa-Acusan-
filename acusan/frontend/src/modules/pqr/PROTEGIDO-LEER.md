# 🔒 DIRECTORIO CONGELADO — MÓDULO PQR (FRONTEND)

Este módulo está **congelado por conflicto en curso** desde 2026-09-08
(tag `pqr-congelado-v1`). A diferencia de Permisos y Radicados, NO está
congelado por estable: hay trabajo sin revisar y un conflicto pendiente
de resolver. Además, la página está bloqueada para usuarios
(`PQR_BLOQUEADO` en `router/index.js`).

**No edite ningún archivo de esta carpeta** (ni vistas, ni componentes,
ni servicios). Cada archivo tiene un hash de integridad registrado y
cualquier cambio dispara el verificador, bloquea los commits y muestra
un cartel de alerta al arrancar el sistema.

Procedimiento cuando el conflicto se resuelva: ver `PROTECCION-PQR.md`
en la raíz del repositorio.

— Gestión Operativa Acuasan
